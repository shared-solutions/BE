package friend.spring.repository;

import friend.spring.domain.General_poll;
import org.springframework.data.jpa.repository.JpaRepository;

public interface General_PollRepository extends JpaRepository<General_poll, Long> {
}
