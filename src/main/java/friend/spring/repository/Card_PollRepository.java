package friend.spring.repository;

import friend.spring.domain.Card_poll;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Card_PollRepository extends JpaRepository<Card_poll, Long> {
}
